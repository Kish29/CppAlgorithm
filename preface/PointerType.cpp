//
// Created by 蒋澳然 on 2021/7/7.
//

/*int main() {
    // 指针常量
    int a = 9;
    int b = 19;
    int *const ca = &a;
    // ca = &b;     // ERROR 指针常量不能再次赋值
    // 常量指针
    const int c = 29;
    const int *cc = &a;
    const int *cc2 = &c;
    // *cc = 100;  // ERROR    cc指针被定义为指向常量的指针，无法修改指向的值
    // *cc2 = 100;  // ERROR    同理cc2指针被定义为指向常量的指针，无法修改指向的值
    // 常指针常量
    const int *const ccc = &c;
    // *ccc = 99;  // ERROR    ccc指针被定义为指向常量的指针常量，无法修改指向的值
    int d = 999;
    // ccc = &d;   // ERROR    ccc指针被定义为指向常量的指针常量，无法修改指针指向的对象
}*/

